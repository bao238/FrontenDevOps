version: '3.8'

services:
  frontend-news-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: frontend-news-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
